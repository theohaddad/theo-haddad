#include <stdio.h>

int main(int argc, char *argv[])
{
	int array[] = { 34, 5, 2, -3, -23, 34, -342 };
	int max = array[0];
	int smax = array[0];
	int i;

	for(i = 0; i < 7; i++){
		if(array[i] > max){
			max = array[i]
		}
	}

	for(i = 0; i < 7; i++){
		if(array[i] < ){
			max = array[i]
		}
	}

